---
import { getLangFromUrl, useTranslations } from "../i18n/utils"
import Back from '../../src/assets/Local.jpg';
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---


<html>
    <body>
        <div class="relative w-full h-screen bg-white">
            <!-- Imagen de fondo -->
            <img src={Back.src} alt="Descripción de la imagen" class="object-cover w-full h-full filter brightness-[.7] -translate-y-16">
        
            <!-- Cuadro de texto en la parte inferior derecha -->
            <div class="absolute top-24 right-[87px] text-white mr-6">
                <h2 class="text-6xl font-bold  p-4">CADSS</h2>
                <h2 class="text-6xl font-bold text-center">{t('bg-local')}</h2>
            </div>
        </div>
    </body>
</html>